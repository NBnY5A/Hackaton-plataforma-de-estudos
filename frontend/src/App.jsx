import { Outlet } from "react-router-dom";

function App() {
  return (
    <div>
      {/* aqui entraria navbar, providers, etc — quando tiver */}
      <Outlet />
    </div>
  );
}

export default App;
